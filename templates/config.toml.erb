[template]
src = "<%= @name %>.tmpl"
dest = "/etc/nubis-config/<%= @name %>.<%= @format %>"
prefix = "<%= @prefix %>"
keys = [
    "/config"
]

<% if @reload %>
reload_cmd = "<%= @reload %>"
<% end %>
